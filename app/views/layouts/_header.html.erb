<div class="header">
	<% if logged_in? %>
	<em>You are logged in as: </em><%= current_user.email %>
	<%= button_to "Log Out", session_url, method: :delete %>
	<% else %>
	You're not logged in!
	<% end %>
</div>